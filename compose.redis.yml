services:
  redis:
    image: redis:7.2.15-bookworm
    ports:
      - "${DREAM_REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 1s
      retries: 30
