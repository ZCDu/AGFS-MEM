# Keep the build context small and free of anything host-specific.
.venv/
__pycache__/
*.pyc
.pytest_cache/
# Local disk-mode bucket: copying it in would bake one machine's data into
# the image.
local_bucket/
# Credentials belong in the environment at runtime, never in a layer.
.env
.git/
tests/
*.zip
